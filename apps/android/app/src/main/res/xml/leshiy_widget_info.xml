<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis=0: the widget is never polled on a timer; the VpnService pushes refreshes
     via LeshiyWidgetProvider.requestUpdate() on connect/disconnect, and taps refresh themselves. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_leshiy"
    android:previewImage="@drawable/ic_qs_leshiy"
    android:resizeMode="none"
    android:widgetCategory="home_screen" />
